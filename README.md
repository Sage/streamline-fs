This is a companion module for [streamline.js](https://github.com/Sage/streamlinejs). It fixes the odd callback signature of `fs.exists` so that you can call it as `require('streamline-fs').exists(path, _)`.
